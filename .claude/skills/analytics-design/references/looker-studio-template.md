# [レポート名] 設計

**前提**: [analytics-design-template.md](analytics-design-template.md) で分析設計が完了していること。

## 1. 概要

**関連ドキュメント**: [分析設計ドキュメントへのリンク]

**レポートの目的**: なぜ継続的なモニタリングが必要か

## 2. レポート構成

**レポート名:** ...

| ページ | 時間粒度 | 目的 | 主な利用タイミング |
|-------|---------|-----|------------------|
| ... | ... | ... | ... |

## 3. データソース

分析設計ドキュメントのクエリをベースに、期間フィルタ用の調整を行う。

| データソース名 | 用途 |
|--------------|------|
| ... | ... |

### 3.1 [データソース名 1]

**用途:** ...

**クエリ:**

```sql
...
```

**フィールド定義:**

| フィールド | 型 | 説明 |
|-----------|-----|-----|
| ... | ... | ... |

### 3.2 [データソース名 2]

(複数のデータソースがある場合は追加)

## 4. ページ

### 4.1 [ページ名 1]

**目的:** ...

**利用シーン:** ...

**レイアウト:**

```text
┌─────────────────────────────────────────┐
│ [期間フィルタ]                            │
├──────────────┬──────────────┬───────────┤
│ スコアカード 1 │ スコアカード 2 │ ...       │
├─────────────────────────────────────────┤
│ グラフ 1                                 │
├────────────────────┬────────────────────┤
│ グラフ 2           │ グラフ 3           │
└────────────────────┴────────────────────┘
```

**スコアカード:**

| 指標タイトル | データソース | 期間のディメンション | 指標 | フィルタ | 目標 |
|------------|-------------|-------------------|-----|---------|------|
| ... | ... | ... | ... | ... | ... |

**グラフ:**

[グラフ名] ([グラフタイプ]): `データソース名`

- ディメンション: `フィールド名` (日/月)
- 内訳ディメンション: `フィールド名` (カテゴリ分類用)
- 指標: Record Count / SUM(x) / PERCENTILE(x, 50) など
- フィルタ: `フィールド名` = 'xxx'
- 目的: [このグラフで確認したいこと]

### 4.2 [ページ名 2]

(複数のページがある場合は追加)
